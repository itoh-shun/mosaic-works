---
name: tdd
description: テスト駆動開発ポリシー。red-green-refactorサイクルに従う。
---

# TDD ポリシー

## 必須度
| 変更種別 | テスト要否 |
|---|---|
| BE振る舞い変更 | **必須** — テストなしの実装は品質ゲートでFAIL |
| FEロジック変更 | **推奨** — テストがない場合はレビューで減点 |
| FE UIのみ変更 | 不要（UI視覚検証で代替） |
| 型定義・設定のみ | 不要 |

## Red-Green-Refactor サイクル

全ての実装は以下のサイクルで進めること:

### 1. Red（テストを書く）
- **実装の前に**テストを書く
- テストは期待する振る舞いを記述する（実装方法ではない）
- テストを実行し、**失敗することを確認**する
- 失敗理由が「実装がない」であることを確認する（テスト自体のバグでないこと）

### 2. Green（最小実装）
- テストを通すための**最小限の実装**を書く
- エレガントさは後回し。まず動かす
- テストを実行し、**通過することを確認**する
- 他の既存テストが壊れていないことも確認する

### 3. Refactor（整理）
- テストが通った状態でコードを整理する
- 重複の除去、命名の改善、構造の整理
- リファクタ後もテストが通ることを確認する
- **振る舞いを変えない** — テストが変わらないことが証拠

## テスト品質基準
- 正常系だけでなく異常系・境界値をカバーする
- テスト名は振る舞いを説明する（「何をしたら何が起きる」形式）
- AAA (Arrange-Act-Assert) パターンで構造化する
- テストの可読性を重視する（テスト自体がドキュメントになる）
- モックは最小限に。外部依存のみモックし、内部ロジックはモックしない

## 実行報告
- テストを書いただけでは不可。関連テストを実行し、**コマンドと出力**を報告すること
- コンパイル通過のみの報告は不合格
- テスト失敗がある場合、変更前のベースでも同じテストを実行し、既存不具合か変更起因かを切り分ける
